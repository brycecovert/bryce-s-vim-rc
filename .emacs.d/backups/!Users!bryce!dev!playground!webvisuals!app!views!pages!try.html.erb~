<h2><%= @page.site.name %> > <%= @page.name %></h2>
<h4>Url: <%= @page.url %> </h4>
<h4>Status: <i class="awesome-icon <%= @page.status_icon %>"></i> <%= @page.status.to_s.humanize %></h4>
<script>
function refresh() {
  window.location.reload(true);
}

setTimeout(refresh, 10000);
</script>

<%= render 'triage', :page => @page %>
<% if @page.page_queues.where(:completed => false).any? %>
    <%= link_to [:enqueue, @page.site, @page], class: "btn btn-default enqueue disabled" do %>
        <img src="<%= asset_path 'ajax-loader.gif' %>" />
        Checking...
    <% end %>
<% else %>
    <%= link_to [:enqueue, @page.site, @page], class: "btn btn-default enqueue" do %>
        <i class="awesome-icon icon-play"></i> Check Now
    <% end %>
<% end %>

<%= link_to edit_site_page_path(@page.site, @page), class: "btn btn-default" do %>
<i class="awesome-icon icon-pencil"></i> Edit
<% end %>
